# Rust / Cargo
/target/                 # build artifacts (incremental, deps, debug, release, etc.)
**/target/               # nested target dirs
target/**                # be explicit to catch files already added

# Generated docs and rustdoc artifacts
/target/doc/
target/.rustc_info.json
target/.rustdoc_fingerprint.json

# Editor / IDE
.vscode/
.vscode/*
.idea/
*.iml

# Editor swap / backup files
*~
*.swp
*.swo
.*.sw?
.#*
*.orig

# OS files
.DS_Store
Thumbs.db

# Logs, temp
*.log
tmp/
temp/

# Native libs / build outputs
*.rlib
*.so
*.dylib
*.dll
*.exe

# Debug / build metadata
target/debug/
target/debug/**
target/debug/deps/
target/debug/build/
target/debug/.fingerprint/

# Rust analyzer / editor tool outputs
target/rust-analyzer/
target/flycheck0/

# Large caches / tools
CACHEDIR.TAG

# Coverage / profiling
coverage/
*.profraw
*.profdata

# Node / python artifacts if present
node_modules/
venv/
.env

# Optionally ignore Cargo.lock for libraries (uncomment if desired)
# Cargo.lock